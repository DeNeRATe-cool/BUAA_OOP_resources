> 班级：232112
>
> 学号：22375080
>
> 姓名：杨佳宇轩

## Question 1

1. `private f()`
2. 不能，因为子类不能缩小覆盖方法的权限，即不能 `private` 覆盖 `public`

## Question 2

1. ```java
   sup.field = 0, sup.getField() = 1
   sub.field = 1, sub.getField() = 1, sub.getSuperField() = 0
   ```

2. 不能，因为没有发生覆盖，而只是隐藏

## Question 3

1. ```java
   Base staticGet()
   Derived dynamicGet()
   ```

2. 不能，因为静态属性和静态方法都不会发生覆盖，只是发生隐藏

## Question 4

1. **不能通过编译**，因为方法 `study` 没有发生覆盖，父类中没有，无法通过父类进行调用

   **改动方法：**在父类中加入 `study()` 方法的被覆盖代码，则子类会覆盖父类的方法，父类也就可以进行调用了

   ```java
   public void study() {
       System.out.println("父类studying");
   }
   ```

2. ```java
   parent.name=李四
   child.name=张三
   子类eating
   父类sleeping
   子类eating
   子类sleeping
   子类studying
   ```

3. 对于第1个输出 `parent.name=李四`：非静态变量发生隐藏，向上转型之后输出父类的结果

   对于第2个输出 `child.name=张三`：子类输出自己的变量结果

   对于第3个输出 `子类eating`：方法发生覆盖，向上转型之后仍然输出实例化对象的方法调用结果

   对于第4个输出 `父类sleeping`：父类中该方法为 `static` ，无法发生覆盖，只能是隐藏，在向上转型之后输出父类方法的结果

   对于第5个输出 `子类eating`：子类调用自身的方法

   对于第6个输出 `子类sleeping`：子类调用自身的方法

   对于第7个输出 `子类studying`：方法发生覆盖，向上转型之后仍然输出实例化对象的方法调用结果

## Question 5

> 见代码文件 Question05

## Question 6

![image-20241024195947673](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20241024195947673.png)

## Question 7

![image-20241024200840347](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20241024200840347.png)

## Question 8

> 见代码文件 Question08

**思路：**

首先初始化 `ShapeSequence` 实例化对象，初始化大小为 **6**，之后加入多个实例化的形状变量，输出检测

实例化一个 `Iterator` 迭代器对象，输出当前形状

不断移动，直至移动到最尾部，即无法继续移动为止，判断是否已经到尾

再创建第二个 `Iterator` 迭代器对象，进行移动，并在移动的开头和结尾进行比较这两个对象指向的地址是否 `equals`

**代码：**

![image-20241024205043075](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20241024205043075.png)

![image-20241024205105516](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20241024205105516.png)

**输出：**

![image-20241024205117890](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20241024205117890.png)