> 班级：232112
>
> 学号：22375080
>
> 姓名：杨佳宇轩

## Question 1

![image-20240926185444373](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926185444373.png)

1. 标记的第 4 行会出现编译错误

   原因：**存在尚未初始化的变量 `m`**

2. `m` 需要初始化才能使用，因为 `x` 作为成员变量会被默认初始化为 **0**，而 `m` 为局部变量，需要初始化才可以使用

## Question 2

- a. ：正确
- b. ：正确
- c. ：正确
- d. ：错误，只有 `Overload(int m)` 和 `Overload(double m)` 是构造方法

## Question 3

程序输出：

```java
initialize A1
initialize A2
initialize A3
initialize A4
initialize A5
initialize A6
copy from A6
initialize B1
initialize A8
main begins
initialize A9
initialize A6
copy from A6
initialize B2
initialize A8
main ends
```

## Question 4

1. **可以证明**：因为初始化 `B` 实例时，先执行了静态和非静态的代码块（初始化了 `a6` 成员变量），之后才初始化了 `a8` 变量
2. **可以证明**：因为对于 `B` 类中的两个非静态代码块 `A a6 = new A(6);` 与 `A a7 = new A(a6)` ，程序是先执行了 `A a6 = new A(6)` 输出了 `initialize A6` 才执行 `A a7 = new A(a6)` 输出了 `copy from A6`

## Question 5

在该类被**第一次被加载**的时候执行静态属性的初始化并且其执行顺序是按照它们在类中出现的顺序依次进行初始化。

## Question 6

1. **不能**

   在代码类 `Initialization` 的第一行加入 `static { B.show(); }` ，即在加载 `Initialization` 类的时候先执行 `B` 静态方法的访问，可以发现，`B` 类会进行加载。

   ![image-20240926192900015](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926192900015.png)

2. [ 带有 `static` 关键字的方法、变量、代码块 ] 可以调用**带有 `static` 关键字的方法、变量**

   [ 不带有 `static` 关键字的方法、变量、代码块 ] 可以调用**都可以**

## Question 7

**不可以**，因为 `Singleton` 类的构造函数为 `private`

## Question 8

因 `Singleton` 类的构造函数为 `private` ， 外部无法通过 `new Singleton()` 构造一个新的 `Singleton` 实例，只可以通过 `getInstance()` 来获取 `Singleton` 类被第一次加载时产生的实例，保证了这个类最多只可能存在 **1** 个实例同时存在

## Question 9

只需要通过 `getInstance()` 方法返回的静态实例进行调用即可

![image-20240926194329829](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926194329829.png)

## Question 10

![image-20240926195500144](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926195500144.png)

![image-20240926195446656](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926195446656.png)

![image-20240926195517165](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926195517165.png)

## Question 11

![image-20240926202012611](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926202012611.png)

![image-20240926202034708](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926202034708.png)

![image-20240926202047895](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240926202047895.png)