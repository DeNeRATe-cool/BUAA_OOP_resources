> 班级：232112
>
> 学号：22375080
>
> 姓名：杨佳宇轩

## Question 1

![image-20240912191154247](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912191154247.png)

## Question 2

|  基本类型   | 默认值 |   基本类型    | 默认值 |
| :---------: | :----: | :-----------: | :----: |
| **`byte`**  |   0    | **`boolean`** | false  |
| **`short`** |   0    |  **`char`**   | /u0000 |
|  **`int`**  |   0    |  **`float`**  |  0.0   |
| **`long`**  |   0    | **`double`**  |  0.0   |

```java
public class Main {
    static byte Byte;
    static short Short;
    static int Int;
    static long Long;
    static boolean Boolean;
    static char Char;
    static float Float;
    static double Double;

    public static void main(String[] args) {
        System.out.println(Byte);
        System.out.println(Short);
        System.out.println(Int);
        System.out.println(Long);
        System.out.println(Boolean);
        System.out.println(Char);
        System.out.println(Float);
        System.out.println(Double);
    }
}
```

## Question 3

![image-20240912192126848](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912192126848.png)

```java
public class FindMinMax {
    public static void main(String[] args) {
        double temp, max, min;
        double d1 = 1, d2 = -9.9, d3 = 96.9;

        temp = d1 > d2 ? d1 : d2;
        max = temp > d3 ? temp : d3;

        temp = d1 < d2 ? d1 : d2;
        min = temp > d3 ? d3 :temp;

        System.out.println("max = " + max);
        System.out.println("min = " + min);
    }
}

```

## Question 4

![image-20240912192208387](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912192208387.png)

**解释:**

​	程序从 `main` 函数进入, 开始计算 `b` 的结果.

​	对于 `test1(0)` 由于他也是一个静态函数, 所以可以通过 `main` 函数访问, 输出 `1`, 因为传入的 `0` , `0 < 1` 故第一个函数的返回值为 `true`

​	对于 `test2(2)` 由于它是静态函数可以访问, 输出 `2`,  `2 < 2` 不成立, 故返回值为 `false`

​	由于此时已经可以判定出 `boolean b` 的值为 `false` 了, 故不再进行之后函数的调用, 相当于 `test3()` 函数被**短路**了

## Question 5

1. 源文件名字为: `Student.java`

2. 得到两个字节码文件, 为 `Exam.class` 以及 `Student.class`

3. - 执行 `java Student`: 

     ![image-20240912204714485](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912204714485.png)

   - 执行 `java exam`:

     ![image-20240912204730925](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912204730925.png)

   - 执行 `java Exam.class`

     ![image-20240912204758998](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912204758998.png)

   - 执行 `java Exam`

     ![image-20240912204822418](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912204822418.png)

## Question 6

![image-20240912194044862](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912194044862.png)

```java
class TwoDimensionArray {
    public static void main(String[] args) {
        int[][] b = { { 11 }, { 21, 22 }, { 31, 32, 33 } };
        int sum = 0;
        b[0][0] = 1000;
        for (int i = 0; i < b.length; i++) {
            for (int j = 0; j < b[i].length; j++)
                sum += b[i][j];
        }
        System.out.println("b[0][0] = " + b[0][0] + '\n' +
                "sum = " + sum + '\n' +
                "b.lengh = " + b.length);

        int[][] arr1 = new int[3][4];
        int[][] arr2 = new int[3][];
        int[][] arr3 = { { 0, 1, 2 }, { 3, 4, 5 }, { 6, 7, 8 } };
        int i, j, k = 0;

        // 为arr1[][]赋值，从上到下，从左到右，0-11
        int cnt = 0;
        for (i = 0; i < 3; i++) {
            // arr1为3行4列
            for (j = 0; j < 4; j++) {
                arr1[i][j] = cnt++;
            }
        }

        // 为arr2[][]开辟内存空间，arr2每一行是变长的，元素个数为3、4、5
        for (i = 0; i < 3; i++) {
            // 参考开辟内存空间：new int[2]，开辟2个int的空间
            arr2[i] = new int[3 + i];
        }

        // arr2[][]赋值，从上到下，从左到右，从0开始递增
        cnt = 0;
        for (i = 0; i < 3; i++) {
            for (j = 0; j < arr2[i].length; j++) {
                arr2[i][j] = cnt++;
            }
        }

        // 输出arr1
        System.out.println(" arr1:");
        for (i = 0; i < 3; i++) {
            for (j = 0; j < 4; j++) {
                System.out.print(" " + arr1[i][j]);
                //参考：System.out.print(" " + arr1[i][j]);
            }
            System.out.println();
        }
        System.out.println();

        // 输出arr2
        System.out.println(" arr2:");
        for (i = 0; i < 3; i++) {
            for (j = 0; j < arr2[i].length; j++) {
                System.out.print(" " + arr2[i][j]);
            }
            System.out.println();
        }

        // 输出arr3
        System.out.println(" arr3:");
        for (i = 0; i < 3; i++) {
            // arr3为3行3列
            for (j = 0; j < 3; j++) {
                System.out.print(" " + arr3[i][j]);
            }
            System.out.println();
        }
    }
}
```

## Question 7

**输出结果:** `Jeep好好`

**解释:** 

​	当 `i = 1` 时, 进入第一个case, 但由于没有 `break` 接着进入第二个case, 输出了 `Je`

​	当 `i = 2` 时, 进入第二个case, 输出 `e`

​	当 `i = 3` 时, 进入第三个case, 输出 `p` 并由于没有 `break`, 接着输出 `好`

​	对于 `i = 4` , 直接进入 `default` 输出 `好`

​	最后结果为 `Jeep好好`

## Question 8

**size = 5:**

![image-20240912194621350](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912194621350.png)

**size = 7:**

![image-20240912194650153](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912194650153.png)

```java
import java.util.Scanner;

public class NumberMatrix {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int cnt = in.nextInt(), number = 0;
        for(int i = 0; i < cnt; i++) {
            for(int j = 0; j < cnt; j++) {
                System.out.print(++number + " ");
            }
            System.out.println();
        }
    }
}

```

## Question 9

```java
public class Qsort {
    /**
     * 对一个int数组快速排序，结果是升序的，并且不会开辟额外的数组空间
     * @param arr 待排序的数组
     * @return 升序排列好的 arr，如果 arr == null，则返回 null
     */

    private static void quick_sort(int[] arr, int n, int l, int r) {
        if(l >= r) return;

        int loc = l, temp = 0;
        for(int i = l + 1; i <= r; i++) {
            if(arr[i] < arr[l]) {
                loc++;
                temp = arr[loc];
                arr[loc] = arr[i];
                arr[i] = temp;
            }
        }
        temp = arr[l];
        arr[l] = arr[loc];
        arr[loc] = temp;
        quick_sort(arr, n, l, loc - 1);
        quick_sort(arr, n, loc + 1, r);
    }

    public static int[] qsort(int[] arr) {
        if(arr == null) return null;
        else if(arr.length == 0) return arr;
        quick_sort(arr, arr.length, 0, arr.length - 1);
        return arr;
    }

    public static void main(String[] args) {
        int[] arr = {9, 8, 4, 7, 1, 2};
        arr = qsort(arr);
        for(int item: arr) {
            System.out.print(item + " ");
        }
        System.out.println();
    }
}

```

## Question 10

1. - `1`: 输出内容 `1111, 2222`
   - `2`: 输出内容 `-100, -200`
   - `3` 输出内容 `0, 0`
   - `4` 输出内容 `0, 0`

2. **浅拷贝:** 对基本数据类型进行值传递，对引用数据类型进行引用传递般的拷贝，此为浅拷贝。只是增加了一个指针指向已存在的内存地址

   **深拷贝:** 对基本数据类型进行值传递，对引用数据类型，创建一个新的对象，并复制其内容，此为深拷贝。增加一个指针并且申请了一个新的内存, 使得这个指针指向新的内存

3. 可以先 `new` 一个类, 申请到新的内存, 再将需要深拷贝的元素的每个成员变量赋值给新的这个变量

   > 当然, 还有更加高级的方法 [Java对象的深拷贝](https://blog.csdn.net/zhangzehai2234/article/details/134300926)

4. `String` 的 `==` 比较的是两个字符串的引用地址是否相等, 所以就算两个字符串完全相同, 其地址不同, 该布尔式的结果也是 `false`

   `String` 的 `equals()` 方法比较的就是字符串的内容, 只要字符串完全相同, 就会返回 `true`

## Question 11

![image-20240912202357712](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912202357712.png)

![image-20240912202420511](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912202420511.png)

```java
public class StringCatch {
    /**
     * 将任意个字符串顺序连接，不应该改变任意一个原有参数
     * @param args 字符串们
     * @return args中的字符串顺序连接组成的新字符串
     */
    public static String strscat(String... args) {
        String str = "";
        for(String arg : args) {
            str += arg;
        }
        return str;
    }

    public static void main(String[] args) {
        System.out.println(strscat("a", "b", "cd", "efg"));
    }
}

```

1. **实现思想:** 在实际调用时将传入的可变参数装入一个数组中

   可变参数为**优势**在于调用该方法时候可以放入人一个数量的该类型参数,中间用逗号隔开即可. 就相当于有了有参和无参两个方法, 同样, 可以作为一个不定长数组, 不需要固定新建一个传入的数据变量

2. 可以通过编译, 因为这个相当于提前申请内存, 加载变量到了数组中, 但其实可以去掉

3. ![image-20240912202625907](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912202625907.png)

   ( 无法同时声明 )

4. ![image-20240912202736276](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912202736276.png)

   ( 参数数量不匹配 )

## Question 12

![image-20240912203346945](C:\Users\12298\AppData\Roaming\Typora\typora-user-images\image-20240912203346945.png)

```java
import java.math.BigInteger;
import java.util.Scanner;

public class BigNumberAddition {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        // 读取两个大数
        BigInteger a, b;
        a = scanner.nextBigInteger();
        b = scanner.nextBigInteger();

        // 计算它们的和
        BigInteger sum = a.add(b);

        // 输出结果
//        System.out.println(a);
        System.out.println(sum);
    }
}
```

## Question 13

```java
import java.util.Scanner;
import java.util.ArrayList;
import java.util.List;

public class Test {
    static List<PurchasingAgent> purchasingAgents = new ArrayList<>();
    static List<Chef> chefs = new ArrayList<>();

    private static void addPurchasingAgent(String... args) {
        for(String arg : args) {
            purchasingAgents.add(new PurchasingAgent(arg));
        }
    }

    private static void addChef(String... args) {
        for(String arg : args) {
            chefs.add(new Chef(arg));
        }
    }

    public static void main(String[] args) {
        addPurchasingAgent("姑姑", "妈妈", "我");
        addChef("爸爸", "婶婶");
    }

}

class PurchasingAgent {
    String name;
    List<String> stuff = new ArrayList<>();

    PurchasingAgent() {}

    PurchasingAgent(String name) {
        this.name = name;
    }

    private void buyStuff(String... args) {
        // buy stuffs
    }
}

 class Chef {
    String name;
    List<String> dish = new ArrayList<>();

    Chef() {}

    Chef(String name) {
        this.name = name;
    }
    private void cookDish(String... args) {
        // cook dishes in a queue
    }
 }

```

